name: Trigger auto deployment for capps-backend-hl4m4qb43nd3i

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/capps-backend-hl4m4qb43nd3i-AutoDeployTrigger-b66bbb02-b48b-4821-a6ba-59766b8a5f7c.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CAPPSBACKENDHL4M4QB43ND3I_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CAPPSBACKENDHL4M4QB43ND3I_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CAPPSBACKENDHL4M4QB43ND3I_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          registryUrl: envragvoix2acrhl4m4qb43nd3i.azurecr.io
          registryUsername: ${{ secrets.CAPPSBACKENDHL4M4QB43ND3I_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CAPPSBACKENDHL4M4QB43ND3I_REGISTRY_PASSWORD }}
          containerAppName: capps-backend-hl4m4qb43nd3i
          resourceGroup: rg-env-rag-voix2
          imageToBuild: envragvoix2acrhl4m4qb43nd3i.azurecr.io/capps-backend-hl4m4qb43nd3i:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


